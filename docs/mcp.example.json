{
  "mcpServers": {
    "harbor": {
      "command": "node",
      "args": ["apps/mcp/dist/index.js"],
      "env": {
        "KANBAN_API_URL": "http://localhost:3001",
        "KANBAN_API_TOKEN": "<paste token from Harbor → MCP tokens>"
      }
    }
  }
}
